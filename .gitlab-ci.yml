image: node:6

services:
  - mongo:3.3

before_script:
  - npm install

stages:
  - run
  - test

job_start:
  stage: run
  script:
    - npm run deploy
  only:
    - devel
    - /^feature\/.*$/

job_test:
  stage: test
  script:
    - npm run testing
